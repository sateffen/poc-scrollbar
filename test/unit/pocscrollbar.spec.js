import PocScrollbar from '../../src/pocscrollbar';

describe('PocScrollbar', () => {
    it('should be a class', () => {
        expect(typeof PocScrollbar).toBe('function');
    });

    // Most stuff is tested by the integration tests, so here we don't test a lot
});
